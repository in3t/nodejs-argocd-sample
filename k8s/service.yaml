apiVersion: v1
kind: Service
metadata:
  name: nodejs-argocd-sample
  labels:
    app.kubernetes.io/name: nodejs-argocd-sample
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: nodejs-argocd-sample
  ports:
    - name: http
      port: 80
      targetPort: http
